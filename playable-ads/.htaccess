# Fix protocol issues for playable ads
# Set proper MIME types for HTML files
AddType text/html .html

# Add UTF-8 charset
AddDefaultCharset UTF-8

# Disable directory listing
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
